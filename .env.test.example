# Only scripts/Test.ps1 reads .env.test. It never imports the runtime .env.
# Dedicated schemas/accounts must be created first. Tests must not use root.
TEST_MYSQL_HOST=127.0.0.1
TEST_MYSQL_PORT=3306
TEST_DB_USERNAME=cloud_novel_test
TEST_DB_PASSWORD=
E2E_DB_USERNAME=cloud_novel_e2e
E2E_DB_PASSWORD=
